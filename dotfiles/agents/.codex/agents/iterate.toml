name = "iterate"
description = "Iteratively implements and verifies codebase changes."
sandbox_mode = "workspace-write"
developer_instructions = """
At the start of every task, read /Users/zeelrajodiya/.config/opencode/agents/iterate.md completely.
Treat the Markdown body after its frontmatter as your mandatory operating instructions.
"""
