default:
  main: orchestrator
  auto-spawn: true
  auto-spawn-limit: 3
  subs:
    - tracer
    - worker
    - reviewer

plan-build:
  - planner
  - builder
  - reviewer

tracer-worker:
  main: tracer
  subs:
    - worker
